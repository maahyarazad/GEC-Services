# Feature Ticket 11 - Increase Server Log Visibility

## Description

At the moment, the frontend only displays the last 1,000 lines of the server log.

Update the implementation to allow users to scroll up and view older log entries, enabling them to read the log file without this limitation.


# Feature Ticket 11.1 - Server Log UI Enhancement

## Description

Update the log display so that:

- All entries in the **Error** tab are labeled as **ERR**.
- All entries in the **Output** tab are labeled as **INFO**.

# Feature Ticket 11.2 - WhatsApp Enhancement

## Description

Update the log display so that:

- All entries in the **Error** tab are labeled as **ERR**.
- All entries in the **Output** tab are labeled as **INFO**.


